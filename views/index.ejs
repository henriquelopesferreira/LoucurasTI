<%- include('partials/header.ejs') %>

<%- include ('partials/homenavbar.ejs')%>
<body>

</b><h1>Pagina inicial</h1>

<div class="container">

    <% articles.forEach(article =>{%>
        <div class="card">
            <div class="card-header">
                <h2> <%= article.title %></h2>
            </div>

            <div class="card-body">
                <a class="btn btn-success" href="#">ler artigo</a>
            </div>
        </div>
    
    <% }) %>

</div>

</body>

<%- include('partials/footer.ejs') %>
